# Outputs

This directory stores durable outputs produced by the repository-local harness.

## Rule

Use the subdirectories intentionally.

- `harness-candidates\` is for under-review candidate materials.
- `standard-harness\` is for accepted reusable outputs only.

Do not store daily notes or transient task state in this directory.
