# Reference Layer

This folder contains non-core starter material.

Use it only when the project has actually reached the corresponding lane or complexity.

## Contains

- non-core artifact templates
- optional skills
- deferred extension patterns

## Rule

Nothing under `reference/` is part of the default constitutional load order.
